context("read-alignment")

# test_that("malformed input gives error", {
#     tmp <- tempdir()
#     genomeDir <- file.path(tmp, "starindexdir")
#     starGenomeGenerate(
#         genomeFastaFiles=system.file("extdata/refs/chr1.fa", package="RSTAR"),
#         genomeDir=genomeDir, force=TRUE, execute=TRUE
#     )
#     expect_error(starAlign(readFilesIn=TRUE, genomeDir=))
# })
